function Header() {
    let headingMessage = 'Hello React Word another :)';

    return (
        <header>
            <h1>{headingMessage + '...'}</h1>
            <h2>The bestdfsad</h2>
        </header>
    )
}

export default Header;